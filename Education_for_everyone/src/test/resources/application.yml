spring:
  datasource:
    url: jdbc:h2:mem:dtd_main_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  redis:
    host: 127.0.0.1
    port: 6379
  # Hibernate ddl auto (create, create-drop, validate, update)